   <div class="container">
   <ul class="nav nav-tabs">
    <li class="nav-item">
      <%= link_to "My profile", employer_profile_path, class: "nav-link" %>
    </li>
    <li class="nav-item">
      <%= link_to "My posts", my_posts_path, class: "nav-link" %>
    </li>
    <li class="nav-item">
      <%= link_to "Job applications", job_applications_path, class: "nav-link" %>
    </li>
    <li class="nav-item">
      <%= link_to "My calendar", employer_calendar_path, class: "nav-link active" %>
    </li>
  </ul>
 <%= month_calendar(events: @appointments, attribute: :date) do |date, appointments| %>
  <%= date %>

  <% appointments.each do |appointment| %>
    <div class="appointment" style="width: 200px;">
      <p><%= appointment.name %></p>
  <% end %>
    </div>
<% end %>
</div>
